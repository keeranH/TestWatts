@model Econocom.Model.Models.Benchmark.DeviceAge

@{
    ViewBag.Title = "Delete";
}

<h2>Delete</h2>

<h3>Are you sure you want to delete this?</h3>
<fieldset>
    <legend>Age</legend>

    <div class="display-label">Label</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Label)
    </div>

    <div class="display-label">Minimum</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Minimum)
    </div>

    <div class="display-label">Maximum</div>
    <div class="display-field">
        @Html.DisplayFor(model => model.Maximum)
    </div>
</fieldset>
@using (Html.BeginForm()) {
    <p>
        <input type="submit" value="Delete" /> |
        @Html.ActionLink("Back to List", "Index")
    </p>
}
