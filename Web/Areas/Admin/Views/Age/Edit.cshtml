@model Econocom.Model.Models.Benchmark.DeviceAge

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

<script src="@Url.Content("~/Scripts/jquery.validate.min.js")" type="text/javascript"></script>
<script src="@Url.Content("~/Scripts/jquery.validate.unobtrusive.min.js")" type="text/javascript"></script>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Age</legend>

        @Html.HiddenFor(model => model.Id)

        <div class="editor-label">
            @Html.LabelFor(model => model.Label)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Label)
            @Html.ValidationMessageFor(model => model.Label)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Minimum)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Minimum)
            @Html.ValidationMessageFor(model => model.Minimum)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.Maximum)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.Maximum)
            @Html.ValidationMessageFor(model => model.Maximum)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
