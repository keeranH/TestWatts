@model IEnumerable<Econocom.Model.Models.Benchmark.IDENTITEBANCAIRE>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            IDIDENTITEBANCAIRE
        </th>
        <th>
            CODEBANQUE
        </th>
        <th>
            GUICHET
        </th>
        <th>
            NUMCOMPTE
        </th>
        <th>
            CLERIB
        </th>
        <th>
            IBAN
        </th>
        <th>
            BIC
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDIDENTITEBANCAIRE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CODEBANQUE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.GUICHET)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NUMCOMPTE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CLERIB)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IBAN)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BIC)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
